export type Person = {
    id: string;
    firstName?: string;
    lastName?: string;
    phoneNumber?: string;
}

export type PersonFormData = {
    id: string;
    firstName: string;
    lastName: string;
    phoneNumber: string;
}
